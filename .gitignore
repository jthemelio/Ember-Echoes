# === Godot 4+ Project ===
.godot/
*.tmp
*.translation
# NOTE: Do NOT ignore .import or .uid files in Godot 4, 
# as this breaks resource links for other users.

# === Node.js MCP Bridge ===
node_modules/
# Ignore compiled JS if you want users to build it themselves
# build/ 
# dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env

# === Cursor AI Settings ===
# IMPORTANT: This prevents leaking your local PC paths in mcp.json
.cursor/mcp.json

# === Export Artifacts ===
EchoEmbersExport/
export_presets.cfg
build/